\chapter{Native debugger} \label{c:native-debugger}
%HEVEA\cutname{native-debugger.html}

\section{s:native-debugger-overview}{Overview and usage}

OCaml supports debugging executables built with the native-code compiler "ocamlopt" using
GDB or LLDB on Linux or macOS. We will call this "native debugging", compared to
bytecode debugging supported via "ocamldebug".

OCaml compiler uses the DWARF to describe the debug information it generates.
According to the \href{http://dwarfstd.org/}{DWARF} standard website:

\begin{verbatim}
DWARF is a debugging information file format used by many compilers and debuggers to support source level debugging. It addresses the requirements of a number of procedural languages, such as C, C++, and Fortran, and is designed to be extensible to other languages. DWARF is architecture independent and applicable to any processor or operating system. It is widely used on Unix, Linux and other operating systems, as well as in stand-alone environments.
\end{verbatim}

Within the DWARF standard the compiler specifically uses Call Frame Information (hereafter abbreviated as CFI) to describe
a call stack for OCaml generated assembly and sections of the runtime written in C. e.g. Garbage Collector.

OCaml provides basic support for CFI to describe how one function calls another function. It also defines a calling convention which is architecture specific for how arguments are passed to functions and values are returned. Finally it identifies an area of memory that is allocated on the stack called a "call frame". See [proc.ml] for specific details for your architecture.

Debuggers often need to view or modify the state of any function that is on the call stack, CFI allows the native debugger to find
and inspect this state. CFI information is preserved across the OCaml / C boundary so the OCaml runtime can be debugged.

Name mangling is the process for describing how the OCaml compiler generates symbol names for OCaml language constructs. The format of these symbols is important for debuggers, performance and observability tools, to uniquely identify the source function for a symbol and to do so without resource to the original source code. In the absensce of source mappings, you often need to use mangled names to set breakpoints or they will appear in information the native debugger will display. As such knowing how OCaml performs name mangling is important when debugging OCaml programs. OCaml 5.1.1 uses the a name mangling scheme of `caml<MODULE_NAME>.<FUNCTION_NAME>_<NNN>` where `NNN` is a randomly generated number. Before 5.1.1 the scheme is uses two underscores as the separator e.g. `caml<MODULE_NAME>__<FUNCTION_NAME>_<NNN>`. 

How to compile for debugging?

CFI emission is controlled by the `--enable-cfi` flag which is enabled by default. 

This is sufficient to allow debugging the assembly code generated by `ocamlopt`. To perform source level debugging
code need to be compiled with `-g` flag that record debugging information for exception backtraces, and generates
mappings between assembly and source locations in OCaml. (Note only GDB and LLDB on Linux reliably support this feature).

\section{s:native-debugger-gdb}{Using GDB}
Here we will walk through debugging a simple OCaml program using GDB on Linux, showing the
commands to use and what features are expected to work.

Consider the following program:
\begin{caml_example*}{verbatim}
(* fib.ml *)
let rec fib n = 
  if n = 0 then 0
  else if n = 1 then 1
  else fib (n-1) + fib (n-2)

let main () = 
  let r = fib 20 in 
  Printf.printf "fib(20) = %d" r

let _ = main ()
\end{caml_example*}

Compile this program with ocamlopt:

\begin{verbatim}
$ ocamlopt --version
5.3.0
$ ocamlopt -d -o fib.exe fib.ml
$ ./fib.exe 20
fib(20) = 6765
\end{verbatim}

When run this program prints the 20th Fibonnaci number, using recursion which allows an
opportunity to inspect the call stack. Now startup GDB:

\begin{verbatim}
$ gdb ./fib.exe
\end{verbatim}

Break points can be set either using the mangled names produced by the compiler or using
a combination of file name and line number. 

\begin{verbatim}
(gdb) break camlfib.fib_     # press tab
(gdb) break camlFib.fib_269  # 269 happens to be the random number generated this time
(gdb) Breakpoint 1 at 0x3d160: file fib.ml, line 1.

(gdb) break fib.ml:6         # breakpoint for main function
Breakpoint 2 at 0x3d1d0: file fib.ml, line 6.
\end{verbatim}

Now we can run the program.

Tutorial for GDB - including commands

\section{s:native-debugger-gdb}{Using LLDB}

Tutorial for LLDB - including commands



Printing Values

What should work?

